let person = {
    firstName: "Jan",
    lastName: "Kowalski",
    age: 49
};

let { firstName, lastName, age } = person;

let info = "Imię: " + firstName + ", nazwisko: " + lastName + ", wiek: " + age + " lat.";